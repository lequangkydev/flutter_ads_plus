/// Identifies which AdMob ad format an event / controller refers to.
/// Phân biệt format ad AdMob mà event / controller đang đại diện.
enum AdType {
  native,
  banner,
  interstitial,
  reward,
  appOpen,
}
